{% extends 'base.html.twig' %}

{% block title %}RestaurantPicture{% endblock %}

{% block body %}
    <h1>RestaurantPicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant_picture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ restaurant_picture.name }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ restaurant_picture.file }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurant_picture_index') }}">back to list</a>

    <a href="{{ path('app_restaurant_picture_edit', {'id': restaurant_picture.id}) }}">edit</a>

    {{ include('restaurant_picture/_delete_form.html.twig') }}
{% endblock %}
